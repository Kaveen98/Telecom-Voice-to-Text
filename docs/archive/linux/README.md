# Archived Linux Deployment Files

Linux/systemd deployment files are preserved here for future use, but they are
not part of the current Windows Server realtime deployment branch.

The active deployment target for this branch is Windows Server running:

```powershell
python watcher.py
```

Service files in this archive contain Linux-specific paths, users, and process
assumptions. Review and update them before reusing them on a Linux server.
